name: workflows for learn
on: workflow_dispatch
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
          - name: Print to console
            run: echo Hello GH Action!
          - name: Print a few line
            run: |
              echo First line!
              echo Second line!
          
